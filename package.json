{
    "name": "rita",
    "version": "1.0.0",
    "description": "Teaching Assistant for Equitable Education",
    "scripts": {
        "gui:wake-up": "cd gui && npm install && npm start",
        "gui:dev": "cd gui && npm start",
        "server:wake-up": "source $(conda info --base)/etc/profile.d/conda.sh && conda activate rita && conda env update --file environment.yml --prune && cd server && flask run",
        "server:dev": "source $(conda info --base)/etc/profile.d/conda.sh && conda activate rita && cd server && flask run",
        "my-test": "cd server && flask run"
    },
    "keywords": [],
    "author": "",
    "license": ""
}